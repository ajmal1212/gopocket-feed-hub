{
  "name": "gopocket-feed-hub",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Single upstream market-feed connection, fanned out to every site visitor.",
  "main": "src/index.mjs",
  "scripts": {
    "start": "node src/index.mjs",
    "dev": "node --watch src/index.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
